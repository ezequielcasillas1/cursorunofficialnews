export {
  FEED_CATEGORIES,
  getCategoryApiParam,
  getEmptyFeedMessage,
  OFFICIAL_ONLY_TOOLTIP,
} from '../../../mobile/shared/feed/feedCategories.js';

export { FEED_PAGE_SIZE } from '../../../mobile/shared/feed/feedPagination.js';

/** Max items fetched when client-side search is active (API cap is 200). */
export const FEED_SEARCH_FETCH_LIMIT = 200;
