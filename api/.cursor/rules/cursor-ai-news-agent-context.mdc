---
description: Cold-start onboarding for Cursor AI News agents. NOT PasteCraft. Read AGENT-CONTEXT.md and phase plan first.
alwaysApply: true
---

# Cursor AI News — Agent Onboarding

**Project root:** `C:\Dev\CursorAINews` · **API workspace:** `C:\Dev\CursorAINews\api` · **NOT** `C:\Dev\PasteCraft`

Before any work:

1. Read **`AGENT-CONTEXT.md`** (this folder) — stack, run commands, Android API base, Phase 1 status, blockers.
2. Read **`CURSOR-AI-NEWS-PHASE-PLAN.md`** — one phase at a time; check status boxes.
3. Follow **`.cursor/rules/unofficial-cursor-news-feed-service.mdc`** for phase detail.

Canonical copies also live at `C:\Dev\CursorAINews\docs\` and `C:\Dev\CursorAINews\.cursor\rules\`.

Quick run: `npm run dev` (`:8787`) from this folder · `../mobile/` → `npx expo start --dev-client` · physical Android → `adb reverse tcp:8787 tcp:8787` or `EXPO_PUBLIC_API_BASE`.

Ask user **"Ezequiel, is the implementation successful?"** before SUCCESS logs or commit.
