---
description: Cold-start onboarding for Cursor AI News agents. NOT PasteCraft. Read docs/AGENT-CONTEXT.md and phase plan first.
alwaysApply: true
---

# Cursor AI News — Agent Onboarding

**Root:** `C:\Dev\CursorAINews` · **NOT** `C:\Dev\PasteCraft`

Before any work:

1. Read **`docs/AGENT-CONTEXT.md`** — stack, run commands, Android API base, Phase 1 status, blockers.
2. Read **`docs/CURSOR-AI-NEWS-PHASE-PLAN.md`** — one phase at a time; check status boxes.
3. Follow **`unofficial-cursor-news-feed-service.mdc`** for phase detail.

Quick run: `api/` → `npm run dev` (`:8787`) · `mobile/` → `npx expo start --dev-client` · physical Android → `adb reverse tcp:8787 tcp:8787` or `EXPO_PUBLIC_API_BASE`.

Ask user **"Ezequiel, is the implementation successful?"** before SUCCESS logs or commit.
